/* ===================================================================
   PyASL Dark Theme — Qt Style Sheet
   Brand Palette:
     Dark Purple   #830085
     Bright Coral  #FE565D
     Background    #000000

   Design System:
     Spacing: 4 / 8 / 12 / 16 / 24 / 32
     Border Radius: 4 / 6 / 8
     Button Height: 32px
   =================================================================== */

/* ── Global ─────────────────────────────────────────────────── */
QWidget {
    background-color: #000000;
    color: #e2e8f0;
    font-family: "Segoe UI", "Inter", sans-serif;
    font-size: 13px;
}

QMainWindow {
    background-color: #000000;
}

/* ── Tab Bar ─────────────────────────────────────────────────── */
QTabWidget::pane {
    border: 1px solid #2a0e2b;
    border-radius: 10px;
    background-color: #0d0d0d;
    top: -1px;
}

QTabBar::tab {
    background-color: #0d0d0d;
    color: #64748b;
    padding: 10px 22px;
    border: 1px solid transparent;
    border-bottom: none;
    border-top-left-radius: 8px;
    border-top-right-radius: 8px;
    min-width: 120px;
    font-weight: 500;
}

QTabBar::tab:selected {
    background-color: #1a0a1b;
    color: #e2e8f0;
    border-color: #830085;
}

QTabBar::tab:hover:!selected {
    background-color: #14081a;
    color: #a0aec0;
}

/* ── Buttons ─────────────────────────────────────────────────── */
QPushButton {
    background-color: #830085;
    color: #ffffff;
    border: none;
    border-radius: 6px;
    padding: 6px 16px;
    font-weight: 600;
    font-size: 13px;
    min-height: 28px;
    min-width: 48px;
    text-align: center;
}

QPushButton:hover {
    background-color: #a3339f;
}

QPushButton:pressed {
    background-color: #5e0060;
}

QPushButton:disabled {
    background-color: #1a0a1b;
    color: #4a5568;
}

QPushButton:focus {
    outline: 2px solid #830085;
    outline-offset: 2px;
}

QPushButton[flat="true"] {
    background-color: transparent;
    color: #830085;
    border: 1px solid #830085;
}

QPushButton[flat="true"]:hover {
    background-color: #1a0a1b;
}

QPushButton[danger="true"] {
    background-color: #4a0a0c;
    color: #FE565D;
}

QPushButton[danger="true"]:hover {
    background-color: #5c0d10;
}

QPushButton[success="true"] {
    background-color: #14532d;
    color: #86efac;
}

QPushButton[success="true"]:hover {
    background-color: #166534;
}

/* ── Toolbar ─────────────────────────────────────────────────── */
QToolBar {
    background-color: #0d0d0d;
    border-bottom: 1px solid #2a0e2b;
    padding: 8px 12px;
    spacing: 8px;
}

QToolBar QToolButton {
    background: transparent;
    border-radius: 6px;
    padding: 4px 8px;
    color: #94a3b8;
    font-size: 12px;
}

QToolBar QToolButton:hover { background-color: #1a0a1b; color: #e2e8f0; }
QToolBar QToolButton:pressed { background-color: #2a0e2b; }

/* Pipeline Builder toolbar buttons — consistent height */
QToolBar#pipelineToolbar QPushButton {
    min-height: 28px;
    max-height: 32px;
    padding: 4px 16px;
    font-size: 13px;
}

/* ── Line Edits ──────────────────────────────────────────────── */
QLineEdit, QTextEdit, QPlainTextEdit {
    background-color: #111111;
    border: 1px solid #2a0e2b;
    border-radius: 6px;
    padding: 6px 10px;
    color: #e2e8f0;
    selection-background-color: #830085;
}

QLineEdit:focus, QTextEdit:focus, QPlainTextEdit:focus {
    border-color: #830085;
}

/* Palette search box — refined sizing */
QLineEdit#paletteSearch {
    padding: 6px 12px;
    border-radius: 6px;
    font-size: 12px;
    background-color: #111111;
    border: 1px solid #2a0e2b;
}

QLineEdit#paletteSearch:focus {
    border-color: #830085;
    background-color: #0d0d0d;
}

/* ── ComboBox ────────────────────────────────────────────────── */
QComboBox {
    background-color: #111111;
    border: 1px solid #2a0e2b;
    border-radius: 8px;
    padding: 6px 10px;
    color: #e2e8f0;
    min-width: 120px;
}

QComboBox:hover { border-color: #830085; }

QComboBox::drop-down {
    border: none;
    width: 24px;
}

QComboBox QAbstractItemView {
    background-color: #0d0d0d;
    border: 1px solid #2a0e2b;
    color: #e2e8f0;
    selection-background-color: #830085;
    selection-color: #ffffff;
}

/* ── SpinBox ─────────────────────────────────────────────────── */
QSpinBox, QDoubleSpinBox {
    background-color: #111111;
    border: 1px solid #2a0e2b;
    border-radius: 8px;
    padding: 5px 8px;
    color: #e2e8f0;
}

QSpinBox:focus, QDoubleSpinBox:focus { border-color: #830085; }

/* ── CheckBox ────────────────────────────────────────────────── */
QCheckBox {
    spacing: 8px;
    color: #e2e8f0;
}

QCheckBox::indicator {
    width: 16px;
    height: 16px;
    border: 2px solid #4a5568;
    border-radius: 4px;
    background: #111111;
}

QCheckBox::indicator:checked {
    background-color: #830085;
    border-color: #830085;
    image: url(none);
}

/* ── GroupBox ────────────────────────────────────────────────── */
QGroupBox {
    border: 1px solid #2a0e2b;
    border-radius: 10px;
    margin-top: 18px;
    padding: 12px 10px;
    font-weight: 600;
    color: #94a3b8;
}

QGroupBox::title {
    subcontrol-origin: margin;
    subcontrol-position: top left;
    padding: 0 8px;
    color: #FE565D;
    left: 12px;
}

/* ── Labels ──────────────────────────────────────────────────── */
QLabel {
    color: #e2e8f0;
}

QLabel[heading="true"] {
    font-size: 16px;
    font-weight: 700;
    color: #e2e8f0;
}

QLabel[muted="true"] {
    color: #64748b;
    font-size: 11px;
}

/* Pipeline Builder palette header */
QLabel#paletteHeader {
    font-size: 13px;
    font-weight: 700;
    color: #FE565D;
    padding: 4px 0px;
    background: transparent;
    border: none;
}

/* Pipeline status footer */
QLabel#pipelineStatus {
    font-size: 11px;
    color: #64748b;
    padding: 8px 12px;
    border-top: 1px solid #2a0e2b;
    background: transparent;
}

/* ── Tables ──────────────────────────────────────────────────── */
QTableWidget {
    background-color: #0d0d0d;
    gridline-color: #2a0e2b;
    border: 1px solid #2a0e2b;
    border-radius: 10px;
    color: #e2e8f0;
    selection-background-color: #1a0a1b;
}

QTableWidget::item { padding: 6px 10px; }
QTableWidget::item:selected { background-color: #1a0a1b; color: #e2e8f0; }

QHeaderView::section {
    background-color: #111111;
    color: #64748b;
    padding: 8px 10px;
    border: none;
    border-bottom: 1px solid #2a0e2b;
    font-size: 11px;
    text-transform: uppercase;
    letter-spacing: 1px;
}

/* ── Scroll Bars — modern thin style ─────────────────────────── */
QScrollBar:vertical {
    background: transparent;
    width: 6px;
    border-radius: 3px;
    margin: 2px 0px;
}
QScrollBar::handle:vertical {
    background: #2a0e2b;
    border-radius: 3px;
    min-height: 30px;
}
QScrollBar::handle:vertical:hover { background: #830085; }
QScrollBar::add-line:vertical, QScrollBar::sub-line:vertical { height: 0; }
QScrollBar::add-page:vertical, QScrollBar::sub-page:vertical { background: transparent; }

QScrollBar:horizontal {
    background: transparent;
    height: 6px;
    border-radius: 3px;
    margin: 0px 2px;
}
QScrollBar::handle:horizontal {
    background: #2a0e2b;
    border-radius: 3px;
    min-width: 30px;
}
QScrollBar::handle:horizontal:hover { background: #830085; }
QScrollBar::add-line:horizontal, QScrollBar::sub-line:horizontal { width: 0; }
QScrollBar::add-page:horizontal, QScrollBar::sub-page:horizontal { background: transparent; }

/* ── Progress Bar ────────────────────────────────────────────── */
QProgressBar {
    background-color: #111111;
    border: none;
    border-radius: 6px;
    height: 8px;
    text-align: center;
    color: transparent;
}

QProgressBar::chunk {
    background: qlineargradient(x1:0, y1:0, x2:1, y2:0,
        stop:0 #830085, stop:1 #FE565D);
    border-radius: 6px;
}

/* ── Splitter ────────────────────────────────────────────────── */
QSplitter::handle {
    background-color: #2a0e2b;
    width: 3px;
    height: 3px;
}

QSplitter::handle:hover { background-color: #830085; }

/* ── Status Bar ──────────────────────────────────────────────── */
QStatusBar {
    background-color: #0d0d0d;
    border-top: 1px solid #2a0e2b;
    color: #64748b;
    font-size: 11px;
    padding: 2px 8px;
}

/* ── List Widget ─────────────────────────────────────────────── */
QListWidget {
    background-color: #0d0d0d;
    border: 1px solid #2a0e2b;
    border-radius: 6px;
    color: #e2e8f0;
    outline: none;
}

QListWidget::item {
    padding: 8px 12px;
    border-radius: 4px;
    margin: 1px 4px;
}
QListWidget::item:selected {
    background-color: #1a0a1b;
    color: #FE565D;
}
QListWidget::item:hover:!selected {
    background-color: #14081a;
}

/* Palette list — refined spacing */
QListWidget#paletteList {
    border: 1px solid #2a0e2b;
    border-radius: 6px;
    background-color: #0d0d0d;
}

QListWidget#paletteList::item {
    padding: 8px 12px;
    border-radius: 4px;
    margin: 1px 4px;
    font-size: 12px;
}

/* ── Tooltip ─────────────────────────────────────────────────── */
QToolTip {
    background-color: #111111;
    color: #e2e8f0;
    border: 1px solid #2a0e2b;
    border-radius: 6px;
    padding: 4px 8px;
    font-size: 12px;
}

/* ── Frame ───────────────────────────────────────────────────── */
QFrame[panelFrame="true"] {
    background-color: #0d0d0d;
    border: 1px solid #2a0e2b;
    border-radius: 10px;
}

/* ── Scroll Area ────────────────────────────────────────────── */
QScrollArea {
    background: transparent;
    border: none;
}
QScrollArea > QWidget > QWidget {
    background: transparent;
}

/* ── Text Browser ───────────────────────────────────────────── */
QTextBrowser {
    background-color: #111111;
    border: 1px solid #2a0e2b;
    border-radius: 8px;
    padding: 6px 10px;
    color: #e2e8f0;
    selection-background-color: #830085;
}

/* ── Menu ────────────────────────────────────────────────────── */
QMenu {
    background-color: #0d0d0d;
    border: 1px solid #2a0e2b;
    border-radius: 8px;
    color: #e2e8f0;
    padding: 4px 0px;
}
QMenu::item {
    padding: 6px 16px;
    color: #e2e8f0;
}
QMenu::item:selected {
    background-color: #1a0a1b;
}
QMenu::separator {
    height: 1px;
    background: #2a0e2b;
    margin: 4px 8px;
}
QMenuBar {
    background-color: #0d0d0d;
    color: #94a3b8;
}
QMenuBar::item:selected {
    background-color: #1a0a1b;
    color: #e2e8f0;
}

/* ── Scroll Bar Corner ──────────────────────────────────────── */
QScrollBar::corner {
    background: transparent;
}

/* ── Tree Widget ────────────────────────────────────────────── */
QTreeWidget {
    background-color: #0d0d0d;
    border: 1px solid #2a0e2b;
    border-radius: 8px;
    color: #e2e8f0;
}
QTreeWidget::item { padding: 4px 8px; }
QTreeWidget::item:selected { background-color: #1a0a1b; color: #FE565D; }
QTreeWidget::item:hover { background-color: #14081a; }

/* ── Stacked Widget ─────────────────────────────────────────── */
QStackedWidget {
    background: transparent;
}
