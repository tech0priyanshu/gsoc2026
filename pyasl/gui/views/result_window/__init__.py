"""
gui/views/result_window — Production-grade Result Analytics Dashboard.

Components:
- ResultWindow — main container
- ResultHeader — header bar with save/export
- ResultOverview — dynamic metrics cards
- ResultToolbar — view/viz selectors, filter, search
- ResultTable — production data table
- ResultSummary — dynamic summary view
- ResultCharts — chart container
- ChartWidgets — matplotlib chart implementations
- ChartConfiguration — progressive-disclosure chart config
- FilterBuilder — advanced filter UI
- SearchBar — debounced global search
- RecordDetails — detail side panel
- ComparisonView — original vs processed
- ProcessingDetails — collapsible provenance section
- ExportMenu — unified export UI
- SaveControls — save/save-as
- ResultHistory — history dialog
"""
